/*
 * Minimal strings.h for NML Edge Worker — ARM Cortex-M4 bare metal.
 * nml.c uses strcasecmp() declared here on POSIX; our string.h already
 * provides it, so this header just re-includes string.h for portability.
 */
#ifndef _EDGE_STRINGS_H
#define _EDGE_STRINGS_H

#include <string.h>

#endif /* _EDGE_STRINGS_H */
